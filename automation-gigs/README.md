# Automation services — reviewed launch build

A service business selling a specific automation outcome, with AI-assisted production. No sales are confirmed.

- First offer: a scoped n8n workflow repair ($120) or build ($400).
- One synthetic Shopify example with runnable logic tests.
- Other portfolio workflows are inactive prototypes, not verified customer deployments.
- Read-only, deduplicated email intake creates private order candidates.
- Funded scope must be verified before a model build. Two calls maximum, budget capped, no model tool access.
- Structural validation produces NEEDS_REVIEW, never automatic delivery approval.
- `SETUP_30_MINUTES.md` contains accurate setup and remaining account/credential steps.
- `LAUNCH_PLAN.md` explains the acquisition test and unit economics.

See `tests/` for runnable regression tests. Actual buyer orders are stored outside this public repository. The historical $400 sample was a synthetic brief; $1.1782 is its original generation cost, not profit.
